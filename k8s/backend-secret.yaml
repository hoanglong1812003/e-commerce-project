# NOTE (learning project): secrets are committed in plaintext here for simplicity.
# In a real environment, use sealed-secrets, SOPS, or an external secret manager instead.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: ecommerce
type: Opaque
stringData:
  DATABASE_URL: "postgresql://ecommerce:ecommerce@postgres:5432/ecommerce?schema=public"
  JWT_SECRET: "change-me-to-a-long-random-string"
  ADMIN_PASSWORD: "Admin@123"
  # Get your own free sandbox credentials at https://sandbox.vnpayment.vn
  VNP_TMN_CODE: ""
  VNP_HASH_SECRET: ""
